
<div class="container">

  <div class="row col-md-12">
    <table class="table table-striped">
      <thead>
      <tr>
        <th>Nom</th>
        <th>Email</th>
      </tr>

      </thead>
      <tbody>
      <% @reservations.each do |r| %>
          <tr>
            <td><%= r.name %></td>
            <td><%= r.email%></td>
          </tr>

      <% end %>
      </tbody>
    </table>

  </div>
</div>

